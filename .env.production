# Vite uses this file for `vite build` mode, even when you deploy to the dev domain.
# Keep this empty so the browser uses same-origin /api/... paths on dev-adm-ui.dataudipi.com.
# The EC2 host nginx should proxy /api/ and /static/ from dev-adm-ui.dataudipi.com to the backend.
VITE_API_URL=
